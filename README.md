# TruHub Business Toolkit

This repository contains the TruHub Business Toolkit - an offline-first, client-only business management app built with React, TypeScript, Vite and TailwindCSS.

Quick start:

1. npm install
2. npm run dev

Build:

npm run build

The app stores all data in localStorage and exports/imports JSON/PDF for backups and exchange.
