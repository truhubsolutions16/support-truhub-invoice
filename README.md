# TruHub Business Toolkit

This repository will contain the TruHub Business Toolkit - an offline-first, client-only business management app built with React, TypeScript, Vite and TailwindCSS.

I created this initial README to initialize the default branch so the project scaffold can be pushed in a new branch.

Next steps (automated by Copilot once you confirm):
- Create a feature branch `truhub-business-toolkit/init` with the full project scaffold.
- Push all source files, configs and assets to that branch in a single commit.

Run locally after scaffold is pushed:
1. npm install
2. npm run dev

If you confirm, I will create the full project on the `truhub-business-toolkit/init` branch now.
